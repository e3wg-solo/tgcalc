# Инструкции по развертыванию

## Вариант 1: Деплой через Vercel CLI (рекомендуется)

```bash
# Установите Vercel CLI глобально (если еще не установлен)
npm install -g vercel

# Или используйте npx (без глобальной установки)
npx vercel

# Для production деплоя
npx vercel --prod
```

Следуйте инструкциям в терминале для авторизации и настройки проекта.

## Вариант 2: Деплой через веб-интерфейс Vercel

1. Перейдите на https://vercel.com
2. Войдите в аккаунт (или создайте новый)
3. Нажмите "Add New Project"
4. Импортируйте репозиторий GitHub/GitLab/Bitbucket или загрузите папку `dist`
5. Настройки проекта:
   - **Framework Preset**: Vite
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
   - **Install Command**: `npm install`
6. Нажмите "Deploy"

## Вариант 3: Деплой через Git (автоматический)

1. Создайте репозиторий на GitHub/GitLab/Bitbucket
2. Закоммитьте и запушьте код:
   ```bash
   git init
   git add .
   git commit -m "Initial commit: Telegram Stars Calculator"
   git remote add origin <your-repo-url>
   git push -u origin main
   ```
3. Подключите репозиторий в Vercel (см. Вариант 2)
4. Каждый push в main будет автоматически деплоить проект

## Вариант 4: Деплой на Netlify

1. Перейдите на https://netlify.com
2. Drag & drop папку `dist` после выполнения `npm run build`
3. Или подключите Git репозиторий

## Проверка деплоя

После успешного деплоя вы получите URL вида: `https://your-project.vercel.app`

Проект готов к использованию! 🚀
